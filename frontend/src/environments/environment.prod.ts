export const environment = {
  production: true,
  // Same-origin: the Express backend serves this built frontend, so API,
  // Socket.io and /uploads are all reachable via relative paths.
  apiUrl: '/api/v1',
};
